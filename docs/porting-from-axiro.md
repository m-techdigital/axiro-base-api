# Porting from AXIRO
Preserve source folder positions when adding a module. Port migration, model, request, controller/service, routes, FE service/pages and tests. Remove permission/company/project bindings rather than adding compatibility fallbacks.
