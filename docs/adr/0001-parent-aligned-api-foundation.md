# ADR 0001: Parent-aligned API foundation

## Decision
Mini API dùng cùng conventions với AXIRO cha cho response envelope, pagination metadata, FormRequest validation, Resources và thin controllers.

## Boundary
Không port RBAC, company scope, project, reports hoặc accounting vào Mini. Chỉ đồng bộ foundation và quality gates.
